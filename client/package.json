{
  "name": "@esquel/client",
  "version": "0.1.0",
  "private": true,
  "description": "Cliente 3D voxel de Esquel 2027 (Three.js + Vite). Se compila a un bundle estático servido por Hostinger.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@esquel/shared": "*",
    "colyseus.js": "^0.15.26",
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "vite": "^5.4.10"
  }
}
